import React from 'react'
import classes from './Loader.css'

const Loader = props => (
	//loader from loading.io/css
	<div className={classes.center}>
		<div className={classes.Loader}>
			<div /><div />
		</div>
	</div>
)

export default Loader
